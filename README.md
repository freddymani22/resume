# Resume – Freddy M

ATS-optimized resume built with LaTeX (Charter font, single-column).

## Stack
- LaTeX (`article` class)
- Charter font (`charter` package)
- Compiled via Overleaf

## Compile locally
```bash
pdflatex resume.tex
```

## Structure
- `resume.tex` — main source file
- `.gitignore` — ignores LaTeX auxiliary files and compiled PDF
